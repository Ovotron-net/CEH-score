import type {Metadata} from 'next';
import AddAssessment from '@/views/AddAssessment';

export const metadata: Metadata = {
    title: 'Add Assessment | CEH Tracker',
};

export default AddAssessment;
